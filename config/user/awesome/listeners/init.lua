return {
	awesome = require("listeners.awesome"),
	client = require("listeners.client"),
	launcher = require("listeners.launcher"),
	layout = require("listeners.layout"),
  screen = require("listeners.screen"),
	tag = require("listeners.tag"),
}
